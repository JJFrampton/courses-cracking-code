#include <stdio.h>
// *linked_list_implementation*
// WIP

typedef struct Nodes {
  char value[50];
  // Node *next;
  int link;
} Node;

typedef struct LinkedLists {
  Node head;
  Node node;
  insert_node
} LinkedList;

int main() {
  printf("\nstart program\n");
  Node node;
  strcpy(node.value, "one");
  strcpy(node.link, 12);
}
